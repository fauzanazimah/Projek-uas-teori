<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a175410197_fauzan_azimah">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu_dua" android:label=" menu Kamera"/>
        <activity android:name=".Menu_kameran" android:label=" menu Kamera"/>
        <activity android:name=".Menu_leptop" android:label=" menu Leptop"/>
        <activity android:name=".Menu_hp" android:label=" menu hp"/>

    </application>

</manifest>